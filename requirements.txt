# Pinned to a verified-working stack (see also pyproject.toml).
# Streamlit Cloud deploys from THIS file, so it must not float onto an
# untested numpy/pandas/lifelines combination (that is what caused the
# "no types given" multivariable failure). Bump these deliberately, together.
streamlit>=1.40,<2.0
pandas==2.3.3
numpy==2.4.6
lifelines==0.30.3
matplotlib==3.11.1
openpyxl==3.1.5
scikit-learn>=1.4,<2.0
seaborn>=0.13,<1.0
scipy==1.17.1
